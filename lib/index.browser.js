const components = require('./components/index.browser')
const pipelines = require('./pipelines/index.browser')
const utils = require('./utils/index.browser')

module.exports = {
  components,
  pipelines,
  utils
}
